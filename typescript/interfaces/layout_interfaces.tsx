export interface ImenuKeys {
  title: string;
  icon: object;
}
export interface Igrade {
  romanNumber: string;
  gradeName: string;
}

export interface Iuser {
  name: string;
  email: string;
  completed: number;
  imagePath: string;
}